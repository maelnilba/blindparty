import { ONE_SECOND_IN_MS } from "lib/helpers/date";

export const VIEW_SCORE_MS = 2 * ONE_SECOND_IN_MS;
export const GUESS_MS = 2 * ONE_SECOND_IN_MS;
export const TRACK_TIMER_MS = 2 * ONE_SECOND_IN_MS;
